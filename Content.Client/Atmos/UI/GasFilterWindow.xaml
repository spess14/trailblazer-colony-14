<DefaultWindow xmlns="https://spacestation14.io"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
            MinSize="400 250" Title="Filter">
    <BoxContainer Orientation="Vertical" Margin="5 5 5 5" SeparationOverride="10">
        <controls:SwitchButton
            Name="ToggleStatusButton"
            HorizontalAlignment="Left"
            Pressed="True" />

        <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
            <Label Text="{Loc comp-gas-filter-ui-filter-transfer-rate}"/>
            <LineEdit Name="FilterTransferRateInput" MinSize="70 0" />
            <Button Name="SetFilterRate" Text="{Loc comp-gas-filter-ui-filter-set-rate}" Disabled="True"/>
        </BoxContainer>

        <Label Text="{Loc comp-gas-filter-filtered-gases}"/>

        <GridContainer Name="FilteredGasesList" Columns="3">
                <!-- populated via cs code -->
        </GridContainer>

    </BoxContainer>
</DefaultWindow>
