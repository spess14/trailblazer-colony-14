<DefaultWindow xmlns="https://spacestation14.io"
               xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
               xmlns:diegeticUI="clr-namespace:Content.Client._Moffstation.DiegeticUI"
               Title="{Loc 'nuke-user-interface-title'}"
               MinSize="320 310"
               SetSize="320 310">
    <!-- Moff - Adjusted window size above ^ -->
    <BoxContainer Orientation="Vertical"
                  HorizontalExpand="True"
                  VerticalExpand="True">
        <!-- First status label -->
        <PanelContainer Margin="0 0 0 5">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#001c00" />
            </PanelContainer.PanelOverride>
            <Label Name="FirstStatusLabel"/>
        </PanelContainer>
        <!-- Second status label -->
        <PanelContainer Margin="0 0 0 5">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#001c00" />
            </PanelContainer.PanelOverride>
            <Label Name="SecondStatusLabel"/>
        </PanelContainer>
        <BoxContainer Orientation="Horizontal" >
            <GridContainer Columns="3"
                           Name="KeypadGrid">
                <!-- Keypad is filled by code -->
            </GridContainer>
            <BoxContainer Orientation="Vertical"
                          HorizontalExpand="True"
                          Margin="5 0">
                <diegeticUI:MoffDiegeticButtonPhysical Name="EjectButton"
                        Text="{Loc 'nuke-user-interface-eject-button'}"
                        Margin="0 0 0 5"
                        Access="Public"/>
                <diegeticUI:MoffDiegeticButtonPhysical Name="AnchorButton"
                        Text="{Loc 'nuke-user-interface-anchor-button'}"
                        Margin="0 0 0 5"
                        Access="Public"/>
                <diegeticUI:MoffDiegeticButtonPhysical Name="ArmButton"
                        Color="Red"
                        FontColor="White"
                        Text="{Loc 'nuke-user-interface-arm-button'}"
                        Access="Public"
                        StyleClasses="negative"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</DefaultWindow>
