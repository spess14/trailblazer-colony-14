<BoxContainer
    xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.Lobby.UI"
    Orientation="Horizontal"
    HorizontalExpand="True"
    >

    <!-- Moff - I changed this to a normal checkbox because the lock looks stupid -->
    <CheckBox
        Name="LockButton"
        StyleIdentifier="RandomizerLockButton"
        ToggleMode="True"
        VerticalAlignment="Center"
        ToolTip="{Loc 'humanoid-profile-editor-randomizer-lock-tooltip'}"/>
    <Label Name="LockName"/>

</BoxContainer>
